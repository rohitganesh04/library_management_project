version: '3.9'

services:
  web:
    build: .
    command: gunicorn library_management_project.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/code
      - ./db.sqlite3:/code/db.sqlite3
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
